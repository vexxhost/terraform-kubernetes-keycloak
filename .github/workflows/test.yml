name: test

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  terratest:
    runs-on: ubuntu-latest
    steps:
      - uses: cloudposse/github-action-terratest@main
